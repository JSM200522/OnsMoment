name: Build Ons Moment

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter 3.19
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
          cache: true

      - name: Genereer web platform folder
        run: |
          mkdir -p /tmp/flutter_template
          cd /tmp/flutter_template
          flutter create --platforms=web --org=com.onsmoment --project-name=ons_moment .
          cp -r web $GITHUB_WORKSPACE/web

      - name: Fix wakelock import in tablet_scherm
        run: |
          python3 << 'EOF'
          with open('lib/screens/tablet/tablet_scherm.dart', 'r') as f:
              content = f.read()
          if "import 'package:wakelock_plus/wakelock_plus.dart'" not in content:
              lines = content.split('\n')
              last_import_idx = 0
              for i, line in enumerate(lines):
                  if line.startswith('import '):
                      last_import_idx = i
              lines.insert(last_import_idx + 1, "import 'package:wakelock_plus/wakelock_plus.dart';")
              content = '\n'.join(lines)
              with open('lib/screens/tablet/tablet_scherm.dart', 'w') as f:
                  f.write(content)
          EOF

      - name: Voeg Nederlandse locale toe aan main.dart
        run: |
          python3 << 'EOF'
          with open('lib/main.dart', 'r') as f:
              content = f.read()
          if 'flutter_localizations' not in content:
              content = content.replace(
                  "import 'package:flutter/material.dart';",
                  "import 'package:flutter/material.dart';\nimport 'package:flutter_localizations/flutter_localizations.dart';"
              )
          if 'localizationsDelegates' not in content:
              old = "return MaterialApp(\n      title: 'Ons Moment',"
              new = """return MaterialApp(
            title: 'Ons Moment',
            localizationsDelegates: const [
              GlobalMaterialLocalizations.delegate,
              GlobalWidgetsLocalizations.delegate,
              GlobalCupertinoLocalizations.delegate,
            ],
            supportedLocales: const [Locale('nl', 'NL'), Locale('en', 'US')],
            locale: const Locale('nl', 'NL'),"""
              content = content.replace(old, new)
          with open('lib/main.dart', 'w') as f:
              f.write(content)
          print("Nederlandse locale toegevoegd")
          EOF

      - name: Genereer firebase_options.dart
        run: |
          python3 << 'EOF'
          import json
          with open('google-services.json', 'r') as f:
              config = json.load(f)
          client = config['client'][0]
          project_id = config['project_info']['project_id']
          android_api_key = client['api_key'][0]['current_key']
          android_app_id = client['client_info']['mobilesdk_app_id']
          messaging_sender_id = config['project_info']['project_number']
          storage_bucket = config['project_info']['storage_bucket']

          firebase_options = f'''// Automatisch gegenereerd
          import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;
          import 'package:flutter/foundation.dart'
              show defaultTargetPlatform, kIsWeb, TargetPlatform;

          class DefaultFirebaseOptions {{
            static FirebaseOptions get currentPlatform {{
              if (kIsWeb) {{
                return web;
              }}
              switch (defaultTargetPlatform) {{
                case TargetPlatform.android:
                  return android;
                default:
                  return web;
              }}
            }}

            static const FirebaseOptions web = FirebaseOptions(
              apiKey: 'AIzaSyCETrCtK-bdJTyQN3j2i470mKG-SojjpIY',
              appId: '1:1051901056201:web:ee53681d9b90ffa9476918',
              messagingSenderId: '1051901056201',
              projectId: 'onsmonent',
              authDomain: 'onsmonent.firebaseapp.com',
              storageBucket: 'onsmonent.firebasestorage.app',
            );

            static const FirebaseOptions android = FirebaseOptions(
              apiKey: '{android_api_key}',
              appId: '{android_app_id}',
              messagingSenderId: '{messaging_sender_id}',
              projectId: '{project_id}',
              storageBucket: '{storage_bucket}',
            );
          }}
          '''
          with open('lib/firebase_options.dart', 'w') as f:
              f.write(firebase_options)
          EOF

      - name: Update main.dart om firebase_options te gebruiken
        run: |
          python3 << 'EOF'
          with open('lib/main.dart', 'r') as f:
              content = f.read()
          if 'firebase_options.dart' not in content:
              content = content.replace(
                  "import 'package:firebase_core/firebase_core.dart';",
                  "import 'package:firebase_core/firebase_core.dart';\nimport 'firebase_options.dart';"
              )
              content = content.replace(
                  'await Firebase.initializeApp();',
                  'await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);'
              )
              with open('lib/main.dart', 'w') as f:
                  f.write(content)
          EOF

      - name: Pub get
        run: flutter pub get

      - name: Build web release
        run: flutter build web --release --base-href "/OnsMoment/"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy_web:
    needs: build_web
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
